 <form>
    <div class="row">
        <div class="col-6 col-sm-12 col-md-12 col-lg-6">
            <div class="form-group">
            <label for="" style="width:180px">Điểm khởi hành
            <i class="fas fa-plane-departure text-warning pos-relative"></i>
            </label>
            <select name="bookingFrom" class="custom-select" style="width:200px">
                <option value = "null" selected>Open this select menu</option>
                <%if(typeof airPort === 'object'){%>
                <%for(item of airPort){%>
                    <option value = "<%=item.locationCode%>"><%=item.locationName%></option>
                <%}%>
                <%}%>
            </select>
            </div>
            <div class="form-group">
            <label for="" style="width:180px">Điểm đến
            <i class="fas fa-plane-arrival text-warning pos-relative"></i>
            </label>
            <select name="bookingDestination" class="custom-select" style="width:200px">
                <option selected>Open this select menu</option>
                <%if(typeof airPort === 'object'){%>
                <%for(item of airPort){%>
                    <option value = "<%=item.locationCode%>"><%=item.locationName%></option>
                <%}%>
                <%}%>
            </select>
            </div>
            <div class="form-group">
                <label for="dtp_input2" style="padding: 0; width:180px">Thời gian khởi hành :</label>
                <div class="input-group date form_date col-md-10" style="padding: 0; width:220px; display:inline-flex" data-date="" data-date-format="dd MM yyyy" data-link-field="dtp_input2" data-link-format="yyyy-mm-dd">
                    <input class="form-control" size="12" type="text" value="" name="bookingDepartTime" readonly style="padding: .375rem .75rem !important; height:auto; border:1px solid #dedede">
                    &nbsp;
                    <!-- <span class="input-group-addon"><span class="glyphicon fas fa-times" style="font-size: 48px;"></span></span>-->
                    <span class="input-group-addon"><span class="glyphicon fas fa-calendar text-warning" style="font-size: 48px;"></span></span>
                </div>
                <input type="hidden" id="dtp_input2" value="" /><br/>
            </div>
        </div>
        <div class="col-6 col-sm-12 col-md-12 col-lg-6">
            <div class="form-group">
                <label for="" style="width: 210px">Số lượng khách hàng
                <i class="fas fa-users text-warning pos-relative"></i>
                </label>
                <select name="bookingNumberOfCustomer"class="custom-select" style="display: inline-block; width:200px;">
                <option selected>Open this select menu</option>
                <option value="1">One</option>
                <option value="2">Two</option>
                <option value="3">Three</option>
                </select>
            </div>
            <div class="form-group">
                <label for="" style="width: 210px">Hạng ghế
                <i class="fas fa-ticket-alt text-warning pos-relative"></i>
                </label>
                <select name="bookingSeatClass"class="custom-select" style="display: inline-block; width:200px;">
                <option selected>Open this select menu</option>
                <%if(typeof seatType === 'object'){%>
                    <%for(item of seatType){%>
                    <option value = "<%=item.locationCode%>"><%=item.name%></option>
                    <%}%>
                <%}%>
                </select>
            </div>
            <div class="form-group">
                <div class="icon"><span class="flaticon-seats"></span></div>
                <button type="button" class="mt-3 btn btn-primary float-right">
                    <i class="fas fa-search pos-relative" style="font-size: 15px; color:#fff">&nbsp; Tìm chuyến bay</i>
                </button>
                <div class="clearfix"></div>
            </div>
        </div>
    </div>
</form>